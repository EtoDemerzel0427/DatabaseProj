建立一个深度学习软件栈
分为三层：
最上层是onnx，通过onnx实现多种不同框架的模型转换；
中间是web界面，用tensorboard；
最底层是编译器。

最终转为onnx，将onnx文件给编译器，处理后获得该模型准确度（推断），将结果通过tensorboard展现给用户，可以对模型进行编辑，对某些op进行增删改，可以导入要判断的东西，查看准确度。


为什么要用onnx进行转换呢？
不同的人对不同的模型熟悉度不同，相互转换便于理解

在转换的过程中有的模型转换失败，准确度有下降：
不同的框架下对op的定义不同，达到统一很难